innkeeper.env=test

test {
  innkeeperdb = {
    url = "jdbc:postgresql://localhost:5433/innkeeper-test"
    user = "innkeeper-test"
    password = "innkeeper-test"
    driver = org.postgresql.Driver
    connectionPool = disabled
    keepAliveConnection = true
  }
}
